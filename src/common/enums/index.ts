export enum Roles {
  ADMIN = 'admin',
  STAFF = 'staff',
  USER = 'user',
}

export enum Priority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}

export enum Status {
  OPEN = 'open',
  ASSIGNED = 'assigned',
  SCHEDULED = 'scheduled',
  IN_PROGRESS = 'in_progress',
  RESOLVED = 'reserved',
  CLOSED = 'closed',
}
